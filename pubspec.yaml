name: myan_myan_tap
description: "Myan Myan Tap — a Flutter Android reaction game."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.0-0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
